# Issue #18 — Agent Çalışma Raporu

**Üye:** Eren Eyyüpkoca  
**Tarih:** 2026-04-24  
**Branch:** (yerel; öneri: `feature/uye2-issue-18-llm-cache-cost`)  
**Model:** Cursor ile Claude Sonnet 4.5 (geliştirme sırasında)

## 1. Anladığım Görev
LLM çağrılarını TTL’li önbellekle azaltmak, geçici Gemini / rate-limit hatalarında exponential backoff ile yeniden denemek, token ve tahmini maliyeti `LLMResponse.raw` içinde raporlamak ve Streamlit oturumunda sidebar’da özet göstermek.

## 2. Plan (Kabul Kriterlerine Karşılık)
- [x] AC #1 → Paylaşımlı `LLMPromptCache` (`get_shared_prompt_cache`) + `LLMClient.chat()` içinde SHA-256 anahtarlı lookup/store; ikinci çağrıda `_invoke_gemini_generating` tekrarlanmıyor (test: `test_cache_hit`).
- [x] AC #2 → Varsayılan TTL 86400 s; `LLM_CACHE_TTL_SECONDS` ile yapılandırma; `LLMClient(..., cache_ttl_seconds=...)` ile ilk oluşturma önceliği (paylaşımlı önbellek ilk oluşturulurken).
- [x] AC #3 → En fazla 4 deneme; 1s, 2s, 4s bekleme; `google.api_core.exceptions` (ResourceExhausted, ServiceUnavailable, InternalServerError, DeadlineExceeded) + mesaj tabanlı 429/5xx sezgisel eşleme.
- [x] AC #4 → `raw["usage_metadata"]` ve görev metnindeki `raw["usage"]`: `input_tokens`, `output_tokens`, `estimated_cost_usd`; önbellek isabetinde `cache_hit: True` ve sıfır token/maliyet.
- [x] AC #5 → `st.session_state.total_tokens_used` / `total_cost_usd` birikimi `LLMClient` içinde (Streamlit varsa) + `ui/dashboard.py` sidebar satırı. **`app.py` değiştirilmedi** (yasak).

## 3. Değiştirilen / Eklenen Dosyalar
| Dosya | Değişiklik Tipi | Satır Sayısı | Açıklama |
|-------|-----------------|--------------|----------|
| `modules/llm_cache.py` | NEW | ~130 | TTL’li bellek içi önbellek, `make_key`, process-geneli singleton |
| `modules/llm_client.py` | MODIFY | +296 / -74 (git diff) | Önbellek, retry, usage_metadata/usage, maliyet tahmini, Streamlit birikimi, `bypass_cache` |
| `ui/dashboard.py` | MODIFY | +11 | Oturum token/maliyet başlatma + sidebar metni |
| `tests/test_modules.py` | MODIFY | +40 | `TestLLMClient::test_cache_hit` |
| `reports/agent-runs/issue-18-Eren-2026-04-24.md` | NEW | — | Bu rapor |

## 4. Atlanan / Yapılamayan Maddeler
- ROADMAP’te accumulator için `app.py` işaret edilmişti; ben `app.py`’ye dokunamıyorum (ekip kuralı). Sayacı `LLMClient` içinde Streamlit oturumu varsa güncelledim ve `dashboard`’da anahtarları başlattım — pratikte aynı `session_state` alanları kullanılıyor.
- `tenacity` yerine manuel bekleme döngüsü yazdım; ek paket istemedim.
- `diskcache` yerine bellek içi TTL önbelleği kullandım; `requirements.txt` şişmesin diye.

## 5. Test Sonuçları
- Makinede `pytest tests/ -v` koştum: **18 passed**, 0 failed. `google.generativeai` için bir FutureWarning var, testleri etkilemiyor.
- Eklediğim test: `tests/test_modules.py::TestLLMClient::test_cache_hit`

## 6. Dökümantasyonda Fark Ettiğim Sorunlar (DEĞİŞTİRMEDİM)
Rehberi okurken birkaç yerin kodla tam örtüşmediğini gördüm; `docs/`’a dokunmadım, Scrum Master’a not düşüyorum:
- §4.9’daki “Improver henüz app’e bağlı değil” uyarısı bence eski — `app.py` içinde improver döngüsü zaten var.
- Tuzak #13’teki “LLMClientError yakalanmıyor” ifadesi de güncel koda göre zayıf; `process_text` tarafında try/except görüyorum.
- §5.1 ve §4.6’daki ConflictDetector / hata yakalama anlatımı ile gerçek orchestration biraz farklı; dokümanı ileride tek satırda senkron etmek iyi olur.

## 7. Kapanış
Bu raporu sprint teslimi için doldurdum. Commit mesajını sohbette net bir blok olarak paylaşıyorum; `git push` öncesi stage listesinde `modules/llm_cache.py`, `modules/llm_client.py`, `ui/dashboard.py`, `tests/test_modules.py` ve isteğe bağlı bu rapor dosyası olduğundan emin olacağım. `.gitignore` `reports/` klasörünü hariç tutmuyor; raporu repoya ekleyeceksen bilinçli olarak ekle.
