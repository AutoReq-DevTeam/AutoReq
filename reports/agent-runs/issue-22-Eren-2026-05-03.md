# Issue #22 — Çalışma Raporu

**Üye:** Eren Eyyüpkoca  
**Tarih:** 2026-05-03  
**Branch:** Eren-LLM  

## 1. Anladığım Görev
Prompt ve user mesajı üreticilerinin tam metin çıktılarını Syrupy ile snapshot’layıp regresyon testine bağlamak; 20 Türkçe örnek gereksinim seti ve buna karşılık gelen `conflicts` / `gaps` JSON ground-truth dosyaları hazırlamak; takımın kullanabileceği ortak LLM mock fixture’larını `conftest` içine almak; CI’da `tests/regression/` adımını çalıştırmak. ROADMAP’te geçen `docs/prompt_versioning.md` maddesi için `docs/` klasörüne dokunmama kısıtı vardı; bu yüzden o dosyayı ben oluşturmadım.

## 2. Plan (Kabul Kriterlerine Karşılık)
- [x] AC #1 → `tests/golden/sample_requirements/` (20× `.txt`) + `tests/golden/expected/` (20× `.json`)
- [x] AC #2 → `pytest tests/regression/` yalnızca mock/snapshot; canlı LLM yok
- [x] AC #3 → `tests/regression/__snapshots__/test_prompt_snapshots.ambr` (prompt değişince test kırılıyor)
- [x] AC #4 → `mock_llm_normal`, `mock_llm_empty`, `mock_llm_malformed`, `mock_llm_rate_limit`, `mock_llm_with_markdown`
- [x] Görevler → `test_prompt_snapshots.py`, `test_golden_contract.py`, `test_llm_mock_fixtures.py`, `.github/workflows/test.yml`
- [x] `docs/prompt_versioning.md` → yapılmadı — aşağıda §4

## 3. Değiştirilen / Eklenen Dosyalar
| Dosya | Değişiklik | Açıklama |
|-------|------------|----------|
| `requirements.txt` | MODIFY | `syrupy` bağımlılığı |
| `tests/conftest.py` | MODIFY | `sys.path` sırası + 5 mock fixture |
| `tests/golden/sample_requirements/sample_01..20.txt` | NEW | Örnek gereksinim setleri |
| `tests/golden/expected/sample_01..20.json` | NEW | Ground-truth çıktılar |
| `tests/regression/golden_utils.py` | NEW | Golden dosya ve `ParsedDocument` yardımcıları |
| `tests/regression/test_golden_contract.py` | NEW | Örnek sayısı ve JSON şema kontrolü |
| `tests/regression/test_llm_mock_fixtures.py` | NEW | Mock fixture davranış testleri |
| `tests/regression/test_prompt_snapshots.py` | NEW | Prompt builder snapshot testleri |
| `tests/regression/__snapshots__/test_prompt_snapshots.ambr` | NEW | Syrupy çıktısı |
| `.github/workflows/test.yml` | NEW | Regression + tam test job’ları |
| `reports/agent-runs/issue-22-Eren-2026-05-03.md` | NEW | Bu rapor |

## 4. Atlanan / Yapılamayan Maddeler
- **`docs/prompt_versioning.md`:** Proje kuralı gereği `docs/` altına dosya eklemedim. Prosedür metnini SM veya docs yetkisi olan biri ekleyebilir; istersen aynı içeriği `reports/` altında taslak olarak da tutabilirsin.

## 5. Test Sonuçları
- `pytest tests/ -v` → PASS (165 passed, 5 skipped — SRS ile ilgili mevcut skip’ler)
- `pytest tests/regression/ -v` → PASS (113 passed)

## 6. Dökümantasyon Notları (Dosyayı Değiştirmedim)
- Issue #22’de `docs/prompt_versioning.md` isteniyor; benim tarafımda `docs/` yasağı ile çelişki var — SM’e ilettim / iletilecek.
- `AGENT_GUIDE` içinde `req_type` UI düzeltmesi bazı bölümlerde güncellenmiş, §10’da eski “tek satırlık UI bug” maddesi duruyor olabilir; doküman senkronu için not.
