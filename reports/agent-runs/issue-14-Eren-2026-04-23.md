# Issue #14 — Çalışma Raporu

**Üye:** Eren Eyyüpkoca  
**Tarih:** 2026-04-23  
**Branch:** Eren-LLM

## 1. Anladığım Görev

ROADMAP Issue #14: Muğlak ifadeleri (`hızlı`, `kolay` vb.) ön filtre + LLM (few-shot prompt) ile `AnalysisReport.improvements` sözleşmesine uygun `{"original", "improved", "reason"}` dict’lerine dönüştürmek. Sorumluluk alanı `TEAM.md` ile doğrulandı: **Akıllı Analiz (modules/)** — Eren Eyyüpkoca.

Dış kısıt: Bu görev fiche’de geçen `app.py::process_text()` entegrasyonu, kullanıcı talimatı gereği bu çalışmada **uygulanmadı** (yalnızca raporlandı; Üye 1 / Scrum Master koordinasyonu).

## 2. Plan (Kabul Kriterlerine Karşılık)

- [x] AC #1 — `RequirementImprover().improve()` artık `NotImplementedError` fırlatmıyor.
- [x] AC #2 — Dönen dict: `original`, `improved`, `reason` (tümü `str`).
- [x] AC #3 — Yalnızca `vague_keywords` ön kontrolünden geçen metinlerde LLM çağrılıyor; aksi halde `original == improved` ve gerekçe dönüyor.
- [x] AC #4 — "Sistem hızlı olmalı" senaryosu mock cevabında `improved` en az `500` ve `ms` içeriyor (ölçülebilir eşik).
- [x] AC #5 — `TestRequirementImprover` mock testleri geçiyor.
- [ ] **ROADMAP görevi (parça):** `app.py::process_text()` — **uygulanmadı** (aşağıdaki delegasyon; kullanıcı yasağı).

## 3. Değiştirilen / Eklenen Dosyalar

| Dosya | Değişiklik Tipi | Satır Sayısı | Açıklama |
|-------|-----------------|--------------|----------|
| `modules/improver_prompts.py` | NEW | 86 | `CORE_IMPROVER_PERSONA`, `IMPROVEMENT_SYSTEM_PROMPT` (JSON şema + 3 few-shot), `build_improvement_system_prompt`, `build_improvement_user_prompt` |
| `modules/improver.py` | MODIFY | +134 / -16 (yakl.) | `vague_keywords`, ön filtre, `LLMClient` DI, `improve()` tam implementasyon, Loguru |
| `modules/__init__.py` | MODIFY | +12 / -1 (yakl.) | Improver prompt sabit ve builder’ların export’u |
| `tests/test_modules.py` | MODIFY | +41 (yakl.) | `test_improves_vague_requirement`, `test_skips_llm_when_no_vague_keyword` |
| `reports/agent-runs/issue-14-Eren-2026-04-23.md` | NEW | (bu dosya) | Issue kapanış notu / sprint raporu |

## 4. Atlanan / Yapılamayan Maddeler

- **app.py değişikliği gerekli — Üye 1 (Galip Efe) / orchestration sahibi ile delegasyon:** ROADMAP’teki *her requirement için* `improver.improve(req)` ve sonuçların `report.improvements` listesine eklenmesi, kullanıcı talimatı `app.py` dosyasına dokunulmaması nedeniyle yapılamadı. Entegrasyon için `process_text` içinde döngü + `LLMClientError` politikasının (diğer modüllerle tutarlı) netleştirilmesi gerekir.

- **`pytest tests/ -v` tam set:** `tests/test_core.py` içindeki `test_classifier_raises_not_implemented` ve `test_ner_raises_not_implemented` hâlen FAIL (sınıflar artık implement; AGENT_GUIDE “Bilinen test failure’ları” ile uyumlu). Bu Issue #14 kapsamında değiştirilmedi (`tests/` Agid sorumluluğunda; ayrıca ROADMAP Issue #8).

- **`core/models.py`:** Değişiklik yok; `AnalysisReport.improvements` mevcut sözleşme yeterli.

- **`requirements.txt`:** Yeni bağımlılık eklenmedi (mevcut `google-generativeai` / Loguru yeterli).

## 5. Test Sonuçları

- `pytest tests/ -v` çıktısı: **FAIL** — **2 failed, 15 passed** (süre ~28 s, Win32). Hatalar: `test_classifier_raises_not_implemented`, `test_ner_raises_not_implemented` (önceden var olan, güncel olmayan “stub beklenir” testleri).
- `pytest tests/test_modules.py -v` (Issue #14 ile ilgili modül testleri): **PASS** — **6 passed** (~6.5 s).
- Yeni eklenen testler: `TestRequirementImprover::test_improves_vague_requirement`, `TestRequirementImprover::test_skips_llm_when_no_vague_keyword`.

## 6. Dökümantasyonda Fark Ettiğim Sorunlar (DEĞİŞTİRMEDİM)

- `docs/TEAM.md` “Eren” teknik detayta LangChain / OpenAI-Ollama vb. listeler; repo gerçeği `modules/llm_client.py` üzerinden **Gemini** (`google-generativeai`). Tarihsel/taslak metin; dokunulmadı.
- `docs/AGENT_GUIDE.md` Bölüm 4.9 hâlâ `improver`’ı stub diye anlatıyor olabilir; bu Issue sonrası kod tamamlandı — doküman güncellemesi Scrum kararı.

## 7. Önerilen commit mesajı

```
feat(modules): add improver prompts and LLM-based RequirementImprover

- Add improver_prompts with few-shot and JSON improve/reason contract
- Prefilter vague keywords; skip LLM when no match (token savings)
- Export prompt builders in modules __init__; expand test_modules coverage
```

Değişiklikleri repoya ben göndereceğim; commit mesajı olarak yukarıdakini kullanmayı düşünüyorum.
